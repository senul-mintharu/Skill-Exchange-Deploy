package lk.wedalk.users.service;

/**
 * UserService.java — User Business Logic
 *
 * <p>This file should contain: - @Service annotation - Inject UserRepository - Methods: - UserDto
 * getUserById(Long id) - UserDto getUserByEmail(String email) - List<UserDto> getAllUsers() -
 * List<UserDto> getUsersByRole(Role role) - UserDto updateUser(Long id, UserDto updates) - void
 * suspendUser(Long id) — admin action - void unsuspendUser(Long id) — admin action
 *
 * <p>Purpose: Centralized user management logic used by controllers and other services.
 */
