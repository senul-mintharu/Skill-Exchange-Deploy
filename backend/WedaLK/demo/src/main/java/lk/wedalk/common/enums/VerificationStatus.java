package lk.wedalk.common.enums;

/**
 * VerificationStatus.java — Worker Verification Status Enumeration
 *
 * <p>This file should contain: - An enum with values: NONE, PENDING, APPROVED, REJECTED
 *
 * <p>Purpose: Tracks a worker's identity/skill verification status: - NONE: Worker has not
 * submitted any verification documents - PENDING: Documents submitted, awaiting admin review -
 * APPROVED: Admin has verified and approved the worker - REJECTED: Admin rejected the submission
 * (worker can resubmit)
 */
