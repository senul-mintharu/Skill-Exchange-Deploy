# LF-76 — Restrict quotation acceptance to seekers

**Status:** Completed
**Date:** 2026-03-24

## User Story

As a system, I want to restrict quotation acceptance to seekers, so that workers cannot assign jobs to themselves.

## Acceptance Criteria

- [x] AC 1
- [x] AC 2
- [x] AC 3

## Definition of Done

- [x] Code implemented
- [x] Role-based access enforced
- [x] API tested
- [x] Frontend integrated

## Backend Changes

- Files modified:
  - `src/main/java/lk/wedalk/quotes/controller/QuotationController.java`
  - `src/main/java/lk/wedalk/quotes/service/QuotationService.java`
- Endpoints added:
  - None (existing accept/reject/list endpoints now bind seeker identity from JWT)

## Frontend Changes

- Components modified:
  - `src/services/quoteService.js`
- Routes changed:
  - None

## Notes

- `SecurityConfig` already required `SEEKER` for `POST`/`PATCH` `/api/quotes/*/accept` (401 unauthenticated, 403 worker).
- Seeker id is taken from the JWT principal (email → user id); `seekerId` query params were removed to prevent impersonation.
- `GET /api/quotes/request/{id}` now returns quotations only when the authenticated user owns that request.
